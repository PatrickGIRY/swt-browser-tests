# Requirement

A want to do an application with the Eclipse Standard Widget Toolkit (SWT).

This application must have a document view to display a document in html format.

The document view use an instance of `org.eclipse.swt.browser.Browser` to display the document.
